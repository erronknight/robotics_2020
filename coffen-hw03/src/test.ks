// Sarah Coffen

// select what functions / stage of mission to run and run this.
// for use with quick saves.

run core_functions.
run angles.
run node_functions.
run lko_mun_transfer.

run landing.

core_settings().
// landing().

burn_start().
// node_LKO_to_MUN(-20).
// aim_node_burnvector().

    // mun_capture_node().
    // // warpto(time:seconds + nextnode:eta - 60).
    // execute_node().
    // orbital_decay_to_orbit(ship).

    // warp_along_transfer(4).
    // // mun_orbit_closer_node().
    // // warpto(time:seconds + nextnode:eta - 60).
    // execute_node().

    // orbital_decay_to_land().

    // landing().

    // burn_start().
// // stage.
// gen_staging_control().
// transfer_to_mun().
// stage.
// wait 5.
// stage.
// wait 5.

// warpto(time:seconds + nextnode:eta - 40).

// run exec_node.

// warp_along_transfer().

